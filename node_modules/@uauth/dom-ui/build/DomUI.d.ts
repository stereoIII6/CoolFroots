import AbstractUI, { UIOptions } from '@uauth/abstract-ui';
export interface DomUIOptions {
    className?: string;
    defaultValue: string;
    container?: HTMLElement;
    document?: Document;
    id: string;
    learnMoreLink: string;
    getADomainLink: string;
}
export declare type DomUIConstructorOptions = Partial<DomUIOptions>;
declare class DomUI<T> implements AbstractUI<T> {
    options: DomUIOptions;
    getContainer(): HTMLElement;
    getDocument(): Document;
    constructor(options?: DomUIConstructorOptions);
    getInnerHTML(defaultValue: string): string;
    open(options: UIOptions<T>): Promise<T>;
    private _closer?;
    close(): void;
}
export default DomUI;
